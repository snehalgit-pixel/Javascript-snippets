/* To test javaScript array functions
The codes can be tested on the console

Shifting is equivalent to popping, but it works on the first element instead of the last.
shift() removes the first element from the array.
shift() returns the first element of the array after removing it.

 */
var politicalparties = ["AAP", "Left", "Congress", "BJP", "AIMIM"];
var str1 = politicalparties.shift();
console.log(str1);
console.log(politicalparties);
